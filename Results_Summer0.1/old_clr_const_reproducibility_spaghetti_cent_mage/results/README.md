# Results (latest runs)

Latest spaghetti plot + replicate CSV per gamma value. Each subfolder contains one PDF (spaghetti plot) and one CSV (per-replicate per-constant metrics).

- `gamma_0.0001/` - near-zero uncertainty (from "gamma0.0001New" run)
- `gamma_0.3/` - moderate uncertainty (from "gamma0.3New" run)
- `gamma_0.5/` - high uncertainty
- `gamma_5e-04/` - low uncertainty (only run, no old version)

Spaghetti pages: robustness, median -log10(padj), mean -log10(padj), both-significant count.
